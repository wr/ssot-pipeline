# ssot-pipeline target-repo stub. Generated by `init-target-repo.sh`.
# Don't edit this directly — re-run init-target-repo.sh if anything needs to change.

name: ssot

on:
  repository_dispatch:
    types: [linear-pickup, linear-implement]
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write
  actions: write

jobs:
  linear-pickup:
    if: github.event_name == 'repository_dispatch' && github.event.action == 'linear-pickup'
    uses: ./.github/workflows/linear-pickup.yml
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
      LINEAR_APP_TOKEN: ${{ secrets.LINEAR_APP_TOKEN }}

  linear-implement:
    if: github.event_name == 'repository_dispatch' && github.event.action == 'linear-implement'
    uses: ./.github/workflows/linear-implement.yml
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
      LINEAR_APP_TOKEN: ${{ secrets.LINEAR_APP_TOKEN }}

  pr-review:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/pr-review.yml
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
